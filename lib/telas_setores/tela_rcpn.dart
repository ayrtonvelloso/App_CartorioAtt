import 'package:flutter/material.dart';

class TelaRCPN extends StatelessWidget {
  const TelaRCPN({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Registro Civil'),
      ),
      body: Column(
        
      ),
    );
  }
}
